name: Diagnose parabolic CMNDF second observation v1

on:
  push:
    branches:
      - v1-continuous-f0-contract-v1
    paths:
      - Tests/ParabolicCmndfSecondObservationDiagnostic.cpp
      - .github/workflows/diagnose-parabolic-cmndf-second-observation-v1.yml
  workflow_dispatch:

permissions:
  contents: read

jobs:
  diagnose:
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: v1-continuous-f0-contract-v1
      - name: Dependencies
        run: |
          sudo apt-get update >/dev/null
          sudo apt-get install -y             libasound2-dev libjack-jackd2-dev ladspa-sdk             libcurl4-openssl-dev libfreetype6-dev libfontconfig-dev             libx11-dev libxcomposite-dev libxcursor-dev libxext-dev             libxinerama-dev libxrandr-dev libxrender-dev libwebkit2gtk-4.1-dev             libglu1-mesa-dev mesa-common-dev >/dev/null
      - name: Build and run
        shell: bash
        run: |
          set -euo pipefail
          cp Tests/ParabolicCmndfSecondObservationDiagnostic.cpp Tests/SupervisorContinuityTest.cpp
          cmake -S . -B build-parabolic-cmndf             -DCMAKE_BUILD_TYPE=Release             -DNEUMATON_BUILD_CLEAN_ENGINE_TESTS=ON >/dev/null
          cmake --build build-parabolic-cmndf --config Release --parallel 2             --target SupervisorContinuityTest >/dev/null
          ./build-parabolic-cmndf/clean-engine-tests/SupervisorContinuityTest             | tee /tmp/parabolic-cmndf.log
          grep -q '^PARABOLIC_CMNDF_SECOND ' /tmp/parabolic-cmndf.log
      - name: Summarize
        shell: bash
        run: |
          python3 - <<'PY'
          from pathlib import Path
          import re, statistics
          rows=[]
          for line in Path('/tmp/parabolic-cmndf.log').read_text().splitlines():
              if not line.startswith('PARABOLIC_CMNDF_SECOND '):
                  continue
              d=dict(re.findall(r'(\w+)=([^ ]+)', line))
              for k in ['radius','proposal_error','refined_error','ns']:
                  d[k]=float(d[k])
              d['improves']=int(d['improves'])
              d['pass_1p5']=int(d['pass_1p5'])
              rows.append(d)
          for radius in sorted({r['radius'] for r in rows}):
              sub=[r for r in rows if r['radius']==radius]
              ae=[abs(r['refined_error']) for r in sub]
              print(
                  'PARABOLIC_CMNDF_SUMMARY'
                  f' radius={radius:.1f}'
                  f' cases={len(sub)}'
                  f' improve_fraction={sum(r["improves"] for r in sub)/len(sub):.6f}'
                  f' pass_1p5_fraction={sum(r["pass_1p5"] for r in sub)/len(sub):.6f}'
                  f' median_abs_cents={statistics.median(ae):.6f}'
                  f' worst_abs_cents={max(ae):.6f}'
                  f' median_ns={statistics.median(r["ns"] for r in sub):.3f}'
              )
          PY
