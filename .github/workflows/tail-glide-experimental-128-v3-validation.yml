# Contract synchronized after Live + Experimental harmonic-coordinate V7.
name: Live Experimental Harmonic Coordinate V7 Validation

on:
  push:
    branches: [quality-tail-glide-experimental-128-v3]
    paths:
      - 'Source/ModernPitchEngine.cpp'
      - 'Source/ModernPitchEngine.h'
      - 'Source/PluginProcessor.cpp'
      - 'Source/PluginProcessor.h'
      - 'Source/PluginEditor.cpp'
      - 'Source/SingleWetSpectralRenderer.cpp'
      - 'Source/SingleWetSpectralRenderer.h'
      - 'Source/LivePitchProcessor.h'
      - 'Tests/SingleWetSpectralRendererTest.cpp'
      - 'Tests/CleanModernPitchEngineTest.cpp'
      - 'Tests/SupervisorContinuityTest.cpp'
      - 'Tests/GuiAudibilityTest.cpp'
      - 'Tests/GuiAudioControlContractTest.cpp'
      - 'CMakeLists.txt'
      - '.github/workflows/tail-glide-experimental-128-v3-validation.yml'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: tail-glide-experimental-128-v3-${{ github.ref }}
  cancel-in-progress: true

jobs:
  source-contract:
    name: Single-wet source contract
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4
      - name: Verify topology and scoped changes
        shell: bash
        run: |
          set -euo pipefail
          grep -q 'EXPERIMENTAL_128_V3' Source/ModernPitchEngine.cpp
          grep -q 'case LatencyMode::ultraLive: return 128' Source/ModernPitchEngine.cpp
          grep -q 'GLIDE_STATE_MODEL_V2' Source/ModernPitchEngine.cpp
          grep -q 'LOG_RESPONSE_TAPER_V1' Source/PluginProcessor.cpp
          grep -q 'EXPONENTIAL_AMOUNT_V1' Source/PluginProcessor.cpp
          grep -q 'LIVE_EXPERIMENTAL_HARMONIC_COORDINATE_TRANSPORT_V7' Source/SingleWetSpectralRenderer.cpp
          grep -q 'const bool harmonicGuideValid = frameSize_ <= 256' Source/SingleWetSpectralRenderer.cpp
          grep -q 'transportTargetBin = coherentMeasuredSourceBin + harmonicShiftBins' Source/SingleWetSpectralRenderer.cpp
          grep -q 'harmonicSourceBin \* (safeRatio - 1.0)' Source/SingleWetSpectralRenderer.cpp
          grep -q 'double sourceFundamentalHz = 0.0' Source/SingleWetSpectralRenderer.h
          grep -q 'if (frameSize_ <= 128 && peakValid)' Source/SingleWetSpectralRenderer.cpp
          ! grep -q 'harmonicGuideValid = frameSize_ <= 512' Source/SingleWetSpectralRenderer.cpp
          test "$(grep -c 'SynthesisLayer layer_' Source/SingleWetSpectralRenderer.h)" -eq 1
          ! grep -Eq 'wetMix_|delayedDry|secondaryLayer|TransitionManager|array<SynthesisLayer|dryTrust_|harmonicMask' Source/SingleWetSpectralRenderer.cpp Source/SingleWetSpectralRenderer.h
          ! grep -q 'tailTooWeakForNewDegree' Source/ModernPitchEngine.cpp
          ! grep -q 'maximumTransitionSamples' Source/ModernPitchEngine.cpp
          grep -q 'target_change_becomes_single_wet_musical_glide' Tests/SupervisorContinuityTest.cpp
          grep -q 'tail_micro_glide_does_not_weaken_authority_destination' Tests/SupervisorContinuityTest.cpp
          grep -q 'very_weak_target_change_glides_without_prudence_veto' Tests/SupervisorContinuityTest.cpp
          grep -q 'response_owns_scale_lock_glide_without_mode_prudence' Tests/SupervisorContinuityTest.cpp
          grep -q 'frameSizes { 512, 256, 128 }' Tests/SingleWetSpectralRendererTest.cpp
          grep -q 'experimental_128_shifts_harmonic_family_not_original_grid' Tests/SingleWetSpectralRendererTest.cpp
          grep -q 'experimental_128_harmonic_family_keeps_exact_ratio' Tests/SingleWetSpectralRendererTest.cpp
          grep -q 'live_256_shifts_harmonic_family_not_original_grid' Tests/SingleWetSpectralRendererTest.cpp
          grep -q 'live_256_harmonic_family_keeps_exact_ratio' Tests/SingleWetSpectralRendererTest.cpp

  invariant-tests:
    name: Invariants (${{ matrix.os }})
    needs: source-contract
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-22.04, windows-2022]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - name: Install Linux dependencies
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libasound2-dev libjack-jackd2-dev ladspa-sdk \
            libcurl4-openssl-dev libfreetype6-dev libfontconfig-dev \
            libx11-dev libxcomposite-dev libxcursor-dev libxext-dev \
            libxinerama-dev libxrandr-dev libxrender-dev libwebkit2gtk-4.1-dev \
            libglu1-mesa-dev mesa-common-dev
      - name: Configure Linux
        if: runner.os == 'Linux'
        run: cmake -S . -B build-v3 -DCMAKE_BUILD_TYPE=Release -DNEUMATON_BUILD_CLEAN_ENGINE_TESTS=ON
      - name: Configure Windows
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          cmake -S . -B build-v3 `
            -G "Visual Studio 17 2022" `
            -A x64 `
            -DNEUMATON_BUILD_CLEAN_ENGINE_TESTS=ON
      - name: Build invariant tests
        run: >-
          cmake --build build-v3 --config Release
          --target CleanModernPitchEngineTest VoiceEvidenceAnalyzerTest
          SupervisorContinuityTest SingleWetSpectralRendererTest
          GuiAudibilityTest GuiAudioControlContractTest --parallel 2
      - name: Run invariants Linux
        if: runner.os == 'Linux'
        shell: bash
        run: |
          set -euo pipefail
          ./build-v3/clean-engine-tests/CleanModernPitchEngineTest
          ./build-v3/clean-engine-tests/VoiceEvidenceAnalyzerTest
          ./build-v3/clean-engine-tests/SupervisorContinuityTest
          ./build-v3/clean-engine-tests/SingleWetSpectralRendererTest
          ./build-v3/clean-engine-tests/GuiAudibilityTest
          ./build-v3/clean-engine-tests/GuiAudioControlContractTest
      - name: Run invariants Windows
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          $ErrorActionPreference = 'Stop'
          $tests = @(
            '.\build-v3\clean-engine-tests\CleanModernPitchEngineTest.exe',
            '.\build-v3\clean-engine-tests\VoiceEvidenceAnalyzerTest.exe',
            '.\build-v3\clean-engine-tests\SupervisorContinuityTest.exe',
            '.\build-v3\clean-engine-tests\SingleWetSpectralRendererTest.exe',
            '.\build-v3\clean-engine-tests\GuiAudibilityTest.exe',
            '.\build-v3\clean-engine-tests\GuiAudioControlContractTest.exe'
          )
          foreach ($test in $tests) {
            & $test
            if ($LASTEXITCODE -ne 0) { throw "$test failed with $LASTEXITCODE" }
          }

  windows-vst3:
    name: Windows VST3 candidate
    needs: source-contract
    runs-on: windows-2022
    steps:
      - uses: actions/checkout@v4
      - name: Configure
        shell: pwsh
        run: |
          cmake -S . -B build-vst3 `
            -G "Visual Studio 17 2022" `
            -A x64
      - name: Build VST3
        shell: pwsh
        run: cmake --build build-vst3 --config Release --target Neumaton_VST3 --parallel 2
      - name: Package and checksum
        shell: pwsh
        run: |
          $ErrorActionPreference = 'Stop'
          New-Item -ItemType Directory -Force -Path artifact | Out-Null
          $vst3 = Get-ChildItem build-vst3 -Recurse -Directory -Filter 'Neumaton.vst3' | Select-Object -First 1
          if (-not $vst3) { throw 'Neumaton.vst3 not found' }
          Copy-Item $vst3.FullName artifact\Neumaton.vst3 -Recurse -Force
          Compress-Archive artifact\Neumaton.vst3 -DestinationPath artifact\Neumaton-Harmonic-Coordinate-V7-Windows.zip -Force
          (Get-FileHash artifact\Neumaton-Harmonic-Coordinate-V7-Windows.zip -Algorithm SHA256).Hash.ToLower() | Out-File -Encoding ascii artifact\sha256.txt
      - name: Upload Windows candidate
        uses: actions/upload-artifact@v4
        with:
          name: Neumaton-Harmonic-Coordinate-V7-Windows
          path: |
            artifact/Neumaton-Harmonic-Coordinate-V7-Windows.zip
            artifact/sha256.txt
          retention-days: 14
          if-no-files-found: error
