name: Probe dual-domain F0 vs 2F0 holdout
on:
  push:
    branches: [v1-f0-demolish-and-rebuild-v1]
    paths:
      - Tests/F0WholeNoteExclusiveConjunctionHoldoutProbe.cpp
      - Tests/F0WholeNoteExtendedExclusiveHoldoutProbe.cpp
      - Tests/F0WholeNoteExtendedEightProbe.cpp
      - Tests/F0WholeNoteProgressiveProbe.cpp
      - .github/workflows/probe-f0-exclusive-conjunction-holdout-v1.yml
  workflow_dispatch:
permissions:
  contents: read
jobs:
  probe:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4
        with:
          ref: v1-f0-demolish-and-rebuild-v1
      - name: Build conjunction holdout
        run: |
          sed -i '0,/^int main()$/{s/^int main()$/int progressive_probe_reference_main()/}' \
              Tests/F0WholeNoteProgressiveProbe.cpp
          sed -i '0,/^int main()$/{s/^int main()$/int extended_eight_reference_main()/}' \
              Tests/F0WholeNoteExtendedEightProbe.cpp
          sed -i '0,/^int main()$/{s/^int main()$/int extended_exclusive_reference_main()/}' \
              Tests/F0WholeNoteExtendedExclusiveHoldoutProbe.cpp
          g++ -std=c++17 -O3 -Wall -Wextra -Wpedantic \
              Tests/F0WholeNoteExclusiveConjunctionHoldoutProbe.cpp \
              -o /tmp/f0-exclusive-conjunction-holdout
      - name: Run conjunction holdout
        run: |
          /tmp/f0-exclusive-conjunction-holdout | tee /tmp/f0-exclusive-conjunction-holdout.log
          grep '^EXCLUSIVE_CONJUNCTION_HOLDOUT ' /tmp/f0-exclusive-conjunction-holdout.log
          grep '^EXCLUSIVE_CONJUNCTION_HOLDOUT_SAFE=' /tmp/f0-exclusive-conjunction-holdout.log
