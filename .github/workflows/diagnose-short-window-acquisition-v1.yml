name: Diagnose 10 ms F0 acquisition v1

on:
  push:
    branches:
      - v1-continuous-f0-contract-v1
    paths:
      - Tests/ShortWindowAcquisitionDiagnostic.cpp
      - .github/workflows/diagnose-short-window-acquisition-v1.yml
  workflow_dispatch:

permissions:
  contents: read

jobs:
  diagnose:
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: v1-continuous-f0-contract-v1

      - name: Dependencies
        run: |
          sudo apt-get update >/dev/null
          sudo apt-get install -y             libasound2-dev libjack-jackd2-dev ladspa-sdk             libcurl4-openssl-dev libfreetype6-dev libfontconfig-dev             libx11-dev libxcomposite-dev libxcursor-dev libxext-dev             libxinerama-dev libxrandr-dev libxrender-dev libwebkit2gtk-4.1-dev             libglu1-mesa-dev mesa-common-dev >/dev/null

      - name: Build and run
        shell: bash
        run: |
          set -euo pipefail
          cp Tests/ShortWindowAcquisitionDiagnostic.cpp Tests/SupervisorContinuityTest.cpp
          cmake -S . -B build-short-acquire             -DCMAKE_BUILD_TYPE=Release             -DNEUMATON_BUILD_CLEAN_ENGINE_TESTS=ON >/dev/null
          cmake --build build-short-acquire --config Release --parallel 2             --target SupervisorContinuityTest >/dev/null
          ./build-short-acquire/clean-engine-tests/SupervisorContinuityTest             | tee /tmp/short-acquire.log
          grep -q '^SHORT_ACQUIRE ' /tmp/short-acquire.log
